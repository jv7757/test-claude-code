# 📚 AI多智能体小说创作系统（会话管理版）

基于 **LangGraph** 和 **Gradio** 的多智能体协作小说创作应用。本系统支持多部小说创作、章节知识库参考、SQLite持久化存储，并通过智能审核循环机制确保每章质量。

## ✨ 核心特性

### 🤖 智能体系统
- 🎬 **策划者智能体**：创建包含多个章节的详细大纲（5-8章）
- ✍️ **作家智能体**：逐章创作并润色，参考已完成章节（知识库）
- 🎭 **评论家智能体**：严格评审每章质量并决定是否通过
- 🔄 **智能审核循环**：每章独立审核，不通过时作家根据反馈修改（最多5次）

### 💾 会话管理与持久化
- 📂 **多小说管理**：支持创建和管理多部小说，随时切换
- 💾 **SQLite存储**：所有小说、章节、大纲、评审记录自动保存
- 🔄 **会话切换**：随时加载任意小说继续创作
- 📊 **统计信息**：实时查看创作统计和进度

### 📖 创作功能
- 📖 **多章节支持**：支持创建和管理多个章节，灵活的创作顺序
- 📚 **章节知识库**：后续章节自动参考已完成章节，确保故事连贯性
- 📊 **实时进度显示**：章节目录侧边栏实时显示创作进度
- 🌐 **友好的Web界面**：基于Gradio构建的直观三栏布局

## 🏗️ 系统架构

本系统采用 LangGraph 构建多智能体工作流，支持多章节创作和条件循环：

```
┌─────────────── 第一步：创建大纲 ───────────────┐
│                                               │
│   用户输入主题                                 │
│       ↓                                       │
│   策划者生成多章节大纲（5-8章）                │
│       ↓                                       │
│   解析并保存章节列表                           │
│                                               │
└───────────────────────────────────────────────┘

┌─────────────── 第二步：逐章创作 ───────────────┐
│                                               │
│   用户选择章节号（可任意顺序）                 │
│       ↓                                       │
│   作家创作该章                                 │
│       ├─ 参考：总体大纲                       │
│       ├─ 参考：该章概要                       │
│       └─ 参考：已完成章节（知识库）           │
│       ↓                                       │
│   评论家评审该章                               │
│       ↓                                       │
│   ┌──── 通过审核？────┐                       │
│   │                  │                       │
│  是                 否                        │
│   │                  │                       │
│   ↓                  ↓                        │
│  保存章节     作家修改（最多5次）               │
│               │                               │
│               └──→ 返回评论家                  │
│                                               │
└───────────────────────────────────────────────┘

┌─────────────── 第三步：导出小说 ───────────────┐
│                                               │
│   导出所有已完成章节                           │
│       ↓                                       │
│   生成完整小说文本                             │
│                                               │
└───────────────────────────────────────────────┘
```

### 知识库机制

当创作第N章时，系统会自动提取前面所有已完成章节（第1至N-1章）的内容作为上下文知识库，供作家参考，确保：

- **人物一致性**：角色性格、外貌、背景保持一致
- **情节连贯性**：故事线索、时间线、因果关系合理
- **风格统一性**：语言风格、叙事节奏、氛围保持统一

## 🚀 快速开始

### 1. 环境要求

- Python 3.8+
- OpenAI API Key

### 2. 安装依赖

```bash
# 克隆项目
git clone <your-repo-url>
cd test-claude-code

# 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

### 3. 配置环境变量

复制 `.env.example` 文件并重命名为 `.env`：

```bash
cp .env.example .env
```

编辑 `.env` 文件，填入你的 OpenAI API Key：

```
OPENAI_API_KEY=your_openai_api_key_here
```

### 4. 启动应用

```bash
python app.py
```

应用将在 `http://localhost:7860` 启动。

## 💾 会话管理与持久化存储

### 数据库结构

系统使用SQLite数据库(`novels.db`)存储所有数据：

**novels表**（小说信息）：
- id: 小说ID（主键）
- title: 小说标题
- topic: 小说主题
- overall_outline: 总体大纲
- chapter_outlines: 章节大纲列表（JSON格式）
- total_chapters: 总章节数
- completed_chapters: 已完成章节数
- created_at: 创建时间
- updated_at: 更新时间
- status: 状态（active/completed/archived）

**chapters表**（章节信息）：
- id: 章节ID（主键）
- novel_id: 所属小说ID（外键）
- chapter_number: 章节号
- title: 章节标题
- outline: 章节大纲
- content: 章节内容
- status: 状态（pending/writing/completed）
- revision_count: 修改次数
- approved: 是否通过审核
- feedback: 最新反馈
- all_feedbacks: 所有反馈历史
- created_at: 创建时间
- updated_at: 更新时间

### 会话管理功能

**创建新小说**：
```
1. 在"会话管理"面板的"新建小说"标签中输入小说标题
2. 点击"创建新小说"
3. 系统创建新会话，准备接收主题和大纲
```

**加载现有小说**：
```
1. 在"小说列表"标签中查看所有小说和ID
2. 在"加载小说"标签中输入小说ID
3. 点击"加载小说"
4. 系统自动加载大纲、章节和进度
```

**删除小说**：
```
1. 在"删除小说"标签中输入要删除的小说ID
2. 点击"删除小说"
3. 系统删除小说及其所有章节（级联删除）
```

**自动保存机制**：
- 创建大纲时自动保存到数据库
- 完成章节时自动保存章节内容
- 所有评审记录自动保存
- 进度自动更新

### 使用场景

**场景1：多部小说并行创作**
```
创作小说A第1-3章 → 切换到小说B创作第1-2章 → 回到小说A继续第4章
```

**场景2：中断恢复**
```
创作到第3章时关闭应用 → 几天后启动应用 → 加载小说ID → 继续第4章创作
```

**场景3：多设备同步**
```
复制novels.db文件到另一台电脑 → 启动应用 → 所有小说和进度同步
```

## 📖 使用说明

### 步骤0：会话管理（新增）

首先创建或加载小说会话：

**创建新小说**：
1. 在左侧"会话管理"面板的"新建小说"标签
2. 输入小说标题（如"时空旅行者"）
3. 点击"➕ 创建新小说"
4. 看到"✅ 已创建新会话"提示

**加载现有小说**：
1. 切换到"小说列表"标签查看所有小说
2. 记下要加载的小说ID
3. 在"加载小说"标签输入ID
4. 点击"📂 加载小说"

### 步骤1：创建大纲

1. 在左侧面板输入你的 OpenAI API Key
2. 选择合适的模型（推荐 `gpt-4o-mini` 或 `gpt-4`）
3. 在"小说主题"文本框中描述你的小说
4. 点击"创建大纲"按钮

系统将生成包含5-8个章节的详细大纲，并在右侧显示章节目录。

### 步骤2：逐章创作

1. 在右侧章节目录中查看所有章节
2. 在左侧"选择要创作的章节号"输入框输入章节号（1-N）
3. 点击"创作该章节"按钮
4. 等待AI创作完成（2-5分钟）

**注意**：
- 可以按任意顺序创作章节
- 后续章节会自动参考已完成的前面章节
- 每章独立审核，最多修改5次

### 步骤3：查看结果

在中间内容区的标签页中查看：

- 📋 **总体大纲**：查看完整的多章节规划
- 📊 **创作状态**：当前章节的创作信息（修改次数、审核结果）
- ✨ **当前章节**：刚完成的章节内容
- 💬 **当前评审**：最新的评审反馈
- 📜 **评审历史**：所有评审记录
- 📖 **完整小说**：导出所有已完成章节

### 步骤4：导出小说

所有章节创作完成后，点击"导出完整小说"按钮，系统会按章节顺序生成完整的小说文本。

## 💡 界面布局

```
┌──────────────────────────────────────────────────────────────┐
│                    AI多智能体小说创作系统                       │
├────────────┬──────────────────────────┬─────────────────────┤
│  左侧面板   │      中间内容区           │   右侧章节目录       │
├────────────┼──────────────────────────┼─────────────────────┤
│ ⚙️ 配置    │ 📋 总体大纲              │ 📑 章节目录          │
│  - API Key │ 📊 创作状态              │  ┌─────────────┐   │
│  - 模型    │ ✨ 当前章节              │  │ ✅ 第1章    │   │
│            │ 💬 当前评审              │  │ ✅ 第2章    │   │
│ 📝 主题    │ 📜 评审历史              │  │ ▶️ 第3章    │   │
│  - 输入框  │ 📖 完整小说              │  │ ⏸️ 第4章    │   │
│            │                          │  │ ⏸️ 第5章    │   │
│ 📖 创作    │                          │  └─────────────┘   │
│  - 章节号  │                          │                     │
│  - 创建大纲│                          │ 💡 操作提示          │
│  - 创作章节│                          │                     │
│  - 导出    │                          │                     │
│            │                          │                     │
│ 💡 提示    │                          │                     │
└────────────┴──────────────────────────┴─────────────────────┘
```

## 💡 示例主题

- "一个关于AI觉醒的科幻故事，探讨人工智能与人类情感的关系，包含多个场景转换"
- "古代武侠小说，一个少年从习武到复仇的成长历程"
- "现代都市爱情故事，两个陌生人从相遇到相爱的过程"
- "悬疑推理小说，侦探在孤岛上调查连环谋杀案"
- "奇幻冒险故事，勇者召集伙伴、收集神器、最终拯救王国"

## 🔧 技术栈

- **LangGraph** (>= 0.2.0) - 多智能体编排框架（支持条件分支和循环）
- **LangChain** (>= 0.3.0) - LLM应用开发框架
- **LangChain-OpenAI** (>= 0.2.0) - OpenAI集成
- **Gradio** (>= 4.0.0) - Web界面框架
- **Python-dotenv** (>= 1.0.0) - 环境变量管理

## 📁 项目结构

```
test-claude-code/
├── app.py              # Gradio Web应用（会话管理版）
├── novel_writer.py     # LangGraph多智能体核心逻辑（支持章节知识库）
├── database.py         # SQLite数据库管理模块
├── novels.db           # SQLite数据库文件（自动生成）
├── requirements.txt    # Python依赖包
├── .env.example        # 环境变量示例
├── .gitignore         # Git忽略文件
└── README.md          # 项目文档
```

## 🎯 核心功能

### NovelWritingAgents 类（novel_writer.py）

多智能体系统的核心类，包含：

- `_planner_agent()` - 策划者智能体（生成多章节大纲）
- `_parse_chapter_outlines()` - 解析章节信息
- `_writer_agent()` - 作家智能体（创作章节，参考知识库）
- `_get_previous_chapters_context()` - 获取已完成章节作为知识库
- `_critic_agent()` - 评论家智能体（评审章节质量）
- `_should_continue()` - 条件判断函数（决定循环或结束）
- `create_chapter()` - 创作单个章节
- `get_chapter_list()` - 获取章节列表

### Database 类（database.py）

数据库管理类，提供完整的持久化功能：

**小说操作**：
- `create_novel()` - 创建新小说
- `get_novel()` - 获取小说信息
- `get_all_novels()` - 获取所有小说列表
- `update_novel()` - 更新小说信息
- `delete_novel()` - 删除小说（级联删除章节）
- `update_novel_progress()` - 更新小说进度

**章节操作**：
- `create_or_update_chapter()` - 创建或更新章节
- `get_chapter()` - 获取单个章节
- `get_all_chapters()` - 获取小说的所有章节
- `get_completed_chapters()` - 获取已完成章节（用于知识库）
- `delete_chapter()` - 删除章节

**统计操作**：
- `get_statistics()` - 获取统计信息（小说数、章节数等）

### 章节状态

每个章节包含以下状态：

- **pending** (⏸️ 待创作): 尚未开始创作
- **in_progress** (▶️ 进行中): 正在创作
- **completed** (✅ 已完成): 创作完成并通过审核

### 审核标准

评论家的评分标准：

- **8-10分**：优秀，通过审核 ✅
- **6-7分**：良好，可以通过 ✅
- **4-5分**：一般，需要改进 ❌
- **1-3分**：较差，必须修改 ❌

### Gradio界面

提供直观的三栏布局Web界面：

- **左侧面板**：API配置、主题输入、章节选择、操作按钮
- **中间内容区**：多标签页显示大纲、状态、章节内容、评审等
- **右侧侧边栏**：章节目录，实时显示创作进度

## ⚙️ 自定义配置

### 调整章节数量

编辑 `novel_writer.py:97`：

```python
请规划5-8个章节  # 修改这个数字范围
```

### 调整知识库参考长度

编辑 `novel_writer.py:183`：

```python
context += f"{ch['content'][:500]}..."  # 修改500为其他字数
```

### 调整最大修改次数

编辑 `novel_writer.py:332` 和 `app.py:164`：

```python
if state.get("revision_count", 0) >= 5:  # 修改这个数字
```

### 调整单章字数

编辑 `novel_writer.py:209`：

```python
4. 字数控制在800-1500字  # 修改字数范围
```

## 📊 工作流程示例

### 示例：创作一部5章科幻小说

**第一步：创建大纲**
```
输入主题 → 策划者生成5章大纲 → 显示章节目录
耗时：约30秒
```

**第二步：逐章创作**
```
创作第1章（无知识库参考）→ 2-3分钟
创作第2章（参考第1章）  → 2-4分钟
创作第3章（参考第1-2章）→ 2-4分钟
创作第4章（参考第1-3章）→ 2-4分钟
创作第5章（参考第1-4章）→ 2-4分钟

总耗时：约10-20分钟（取决于修改次数）
```

**第三步：导出小说**
```
点击导出 → 生成包含5章的完整小说
耗时：即时
```

## 🆚 版本对比

### v4.0（当前版本）- 会话管理版

- ✅ **多小说管理**：支持创建和管理多部小说
- ✅ **SQLite持久化**：所有数据自动保存到数据库
- ✅ **会话切换**：随时加载和切换不同小说
- ✅ **数据库管理**：完整的CRUD操作
- ✅ **统计功能**：实时查看创作统计
- ✅ 支持多章节创作（5-8章）
- ✅ 章节知识库参考机制
- ✅ 章节目录侧边栏
- ✅ 每章独立审核循环

### v3.0 - 多章节版

- 支持多章节创作（5-8章）
- 章节知识库参考机制
- 章节目录侧边栏
- 灵活的创作顺序
- 实时进度显示
- ⚠️ 无持久化存储（刷新即丢失）

### v2.0 - 审核循环版

- 3个智能体（策划者、作家、评论家）
- 智能审核循环机制
- 单一完整小说创作

### v1.0 - 线性流程版

- 4个智能体（策划者、作家、编辑、评论家）
- 线性工作流，无循环
- 单次通过，无修改机制

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📝 注意事项

- 确保有稳定的网络连接
- OpenAI API 调用会产生费用，每章约消耗2000-4000 tokens
- 不同模型的效果和成本差异较大：
  - `gpt-4o-mini`: 便宜快速，适合测试，每章约$0.01
  - `gpt-4`: 质量最高，但成本较高，每章约$0.10
- 建议按顺序创作章节，以获得最佳连贯性
- 可以随时中断，下次继续创作剩余章节
- 生成内容仅供参考和娱乐，请合理使用

## 🎓 高级用法

### 非顺序创作

虽然建议按顺序创作，但系统支持任意顺序：

- 先创作第1、3、5章
- 再补充第2、4章
- 后续章节会参考所有已完成章节

### 章节重写

如需重写某章：

- 再次选择该章节号
- 系统会覆盖原有内容
- 评审历史会累积保存

### 大纲调整

如需调整大纲：

- 重新创建大纲会清空所有章节
- 建议先导出已完成内容

## 📄 许可证

MIT License

## 🙏 致谢

- [LangGraph](https://github.com/langchain-ai/langgraph) - 强大的多智能体编排框架
- [LangChain](https://github.com/langchain-ai/langchain) - 优秀的LLM应用开发框架
- [Gradio](https://github.com/gradio-app/gradio) - 简洁易用的ML Web界面框架

---

Made with ❤️ by LangGraph & Gradio
